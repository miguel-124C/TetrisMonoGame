
namespace Tetris.Enums
{
    public enum Direction { Down, Left, Right }
    public enum PieceType { I, J, L, O, S, T, Z }
    public enum BlockColor { Empty, Cyan, Blue, Orange, Yellow, Green, Purple, Red }
    public enum GameStatus { Menu, Playing, Paused, GameOver, Countdown }
}
